import { Typography } from "@mui/material";

export default function Checkout() {
  return (
    <Typography variant="h4" gutterBottom>
        Only Authorized Users Can Checkout
    </Typography>
  )
}